/**
 * Re-exports all types from the domain modules under src/types/.
 * Prefer importing from '../src/types' (barrel) or '../src/types/alert' etc. for domain-specific imports.
 */
export * from './types/index';
